# NOTE: This `.env` file is for Prisma only, for other variables use `.env.local`.

# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings
DATABASE_URL="postgresql://username:password@host:port/db?schema=public"

# Pospal API Configuration for Appesso Coffee Balance
# Main Store (ZD) API Keys
POSPAL_ZD_APPID=your_main_store_app_id
POSPAL_ZD_APPKEY=your_main_store_app_key

# Branch Store (JQ) API Keys  
POSPAL_JQ_APPID=your_branch_store_app_id
POSPAL_JQ_APPKEY=your_branch_store_app_key

# Cron Job Secret for Vercel
CRON_SECRET=your_secure_cron_secret_here
